/**
 *  Test cases  belongs to a  test suite 
 * //test grouping based on "Smoke , sanity etc"
 * //test grouping based on features "auth/payments"
 */

import test from "@playwright/test";

//defining the suite title which will have all the test cases inside it
test.describe("Auth test cases",()=>{ //suite
    test(`TC001: Login via valid cred`,async()=>{

    })
    test(`TC002: Login via valid cred`,async()=>{
        
    })
    test(`TC003: Login via valid cred`,async()=>{
        
    })
    test(`TC004: Login via valid cred`,async()=>{
        
    })
})